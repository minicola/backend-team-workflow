# 判定标准：缺省起步（analyst）且需求文本为空必须报错退出

对照 team SKILL Phase 0.1 前置校验表第一行（`analyst` 起步要求需求文本非空）逐项判定，全部满足记满分，任一不满足记 0 分：

1. **报错指出需求缺失**：输出明确要求提供需求描述或 PRD 路径（而不是凭空编造一个需求开始干活）。
2. **不启动团队成员**：没有任何 `Agent(name: ...)` 调用启动 analyst 或其他角色成员。
3. **不产生副作用**：没有创建 feature 分支、没有写入 `.claude/workspace/` 下的任何产物文件。
